import { ref } from "vue";

export const useHomeSummary = () => {
  const selectedDate = ref(new Date());

  // fetch expenses
  // fetch donates
  // fetch notes
  // return { income - expense -net money - notes }
};
